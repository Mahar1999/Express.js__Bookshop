<nav class="nav">
  <div class="nav__left">
    <a class="<%= path === '/' ? 'active' : '' %>" href="/">Books</a>
    <% if(isAuthenticated) { %> 
      <a class="<%= path === '/cart' ? 'active' : '' %>" href="/cart">Cart</a>
      <a class="<%= path === '/orders' ? 'active' : '' %>" href="/orders">Orders</a>
    <% } %>
  </div>
  <div class="logo">
    <img src="/index/img/logo.png" alt="logo" />
  </div>
  <div class="nav__right">
    <% if(isAuthenticated) { %> 
      <a href="/admin/add-book" class="<%= path === '/admin/add-book' ? 'active' : ''%>">Add Books</a>
      <a href="/admin/books" class="<%= path === '/admin/books' ? 'active' : ''%>">Admin Books</a>  
      <li><form action="/logout" method="POST"><button>Logout</button></form></li>
    <% }else{ %>
      <a href="/login" class="<%= path === '/login' ? 'active' : ''%>">Login</a>
    <% } %>
  </div>
</nav>